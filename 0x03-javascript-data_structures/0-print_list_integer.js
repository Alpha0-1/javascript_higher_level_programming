/**
 * Function to print each integer in a list.
 * @param {Array<number>} list - Array of integers to be printed.
 * @returns {void}
 */
function print_list_integer(list) {
    for (let i = 0; i < list.length; i++) {
        console.log(list[i]);
    }
}
