/**
 * Retrieves an element from an array at the given index.
 * @param {Array} myArray - The input array.
 * @param {number} idx - The index to retrieve.
 * @returns {*} The element at the specified index or undefined if out of bounds.
 */
function element_at(myArray, idx) {
    return myArray[idx];
}
