/**
 * Switches the values of two variables.
 * @param {*} a - First value.
 * @param {*} b - Second value.
 * @returns {Array} Array containing switched values [newA, newB].
 */
function switchValues(a, b) {
    return [b, a];
}

module.exports = switchValues;

