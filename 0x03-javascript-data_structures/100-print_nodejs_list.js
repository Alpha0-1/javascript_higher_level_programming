/**
 * Function to print elements of a list using Node.js.
 * @param {Array<number>} list - Array of integers.
 * @returns {void}
 */
function printNodeJsList(list) {
    list.forEach(element => console.log(element));
}
